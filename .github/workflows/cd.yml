name: Node.js CI/CD

on: 
  push:
    branches:
      - dev

jobs:
  deploy:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version: [20.x]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Deploying to MVPS
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        passphrase: ${{ secrets.PASSPHRASE }}
        script: |
          eval "$(ssh-agent -s)"
          ssh-add /home/${{secrets.USERNAME}}/.ssh/github_key
          cd /home/${{secrets.USERNAME}}/projects/MarvelFanUniverse
          git pull origin dev
          export NVM_DIR=~/.nvm
          source ~/.nvm/nvm.sh
          npm install
          npm run build
          cd build
          cp -rT . /var/www/kyrylofolio.pro/html/mfu